# Runtime data (SQLite database lives here)
data/

# Vendored ML runtime + model (fetched via `npm run setup`)
public/vendor/

# Editor / tooling
.specstory/
.serena/
.playwright-mcp/
.DS_Store
*.log
node_modules/

# graphify knowledge graph — derived, rebuild with graphify.watch
graphify-out/
worker/.bin/
